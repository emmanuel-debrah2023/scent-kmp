package ui.components

/** Value/label pairs for a listing's `condition` field. Shared between the marketplace
 *  filter sheet and the create/edit listing form so both agree on the vocabulary. */
val CONDITION_OPTIONS = listOf("NEW" to "New", "USED" to "Used", "DECANT" to "Decant", "SAMPLE" to "Sample")
